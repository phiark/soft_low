# Notebooks

Use notebooks for exploration only.

Any stable conclusion, metric definition, or workflow discovered here must be promoted back into:

- `docs/` for normative guidance
- `records/` for experiment evidence
- `src/` for reusable implementation

